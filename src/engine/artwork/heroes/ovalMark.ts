/**
 * Oval hero — Art deco layered medallion (sunburst + rings + diamond + flacon).
 * User veto: never paint. Callers keep the type; markup is always empty.
 */
export function paintOval(_cx: number, _cy: number, _r: number, _accent: string): string {
  void _cx
  void _cy
  void _r
  void _accent
  return ''
}
