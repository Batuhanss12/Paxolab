# Studio knowledge — outcome prefer

**Tarih:** 16 Eyl 2026  
**Kaynak:** `docs/PAXOLAB_DI_POST_D4_AUDIT.md` optional Knowledge  
**DI merge değil.** Overlay / VL / critic kazanan yok. 18 golden front hash durur (boş store).

## Gap

C7 “fazla yoğun” **kaçınma** yolunu kanıtladı. Beğenilen yüz (4★ / dışa aktarma) stüdyo arketipini **tercih** olarak biriktirmiyordu: observation id `studio0` `outcome` kaynağı sayılmıyordu; aynı generate kit `avoid-motif` / `director-cue` de yazıyordu. Kit motif stüdyo DNA’sında yok.

## After

- Stüdyo outcome yalnız `studio-archetype` + `studio-background` (prefer ≥4★ / export, avoid ≤2★)
- Kit generate hâlâ cue + avoid-motif yazar
- Stüdyo “çok klasik” avoid-motif yazmaz; boyanan arketipten kaçınır
- İki bağımsız 5★ botanik kahve → sonraki kahve `botanical-card` (tek yıldız kural açmaz)

## Kanıt

`src/engine/studio/studioLearning.test.ts` (outcome prefer) · C7 suite · `studioGolden.test.ts` (18) · `learningGate.test.ts` · tam suite 468 geçti / 1 baseline fail (`phase15` `/hex/i`) / 469 toplam
