# Paxolab SEO (TR)

**Superseded by** [`PAXOLAB_SEO.md`](./PAXOLAB_SEO.md) — covers TR (default at root) + EN (`/en`) with hreflang.

TR remains the default locale without a `/tr` prefix. See the bilingual route map in `PAXOLAB_SEO.md`.
