# FORMA studio craft (S1–S5)

Additive slice after v3.6. Official planned track remains **H0–D6 / 98%**. This is not D7, catalog, or PDF/X.

| Slice | Intent | Painter / gate |
| --- | --- | --- |
| S1 | Optical lockup + crop | 3+ word brands wrap. Variation consumes `heroZone.y` + `crop.heroCrop`. Set 0 kit Y unchanged (perfume crest `0.148`). |
| S2 | Variation sets | StyleBar **6 yeni tasarım** (already shipped). |
| S3 | Grammar illustration | Primitives only on set 1+, `graphic-push`, or smoke `force-overload`. Luxury perfume set 0 = `[]`. No eco leaf + leafStamp double. |
| S4 | Sector craft | Perfume luxury type ramp unchanged (`displayMm` 9.1). Food / electronics / cream get a quiet voice. Perfume-only jewelry: top foil hairline + corner diamonds. |
| S5 | Print proof | `glue-art`, `type-fit`, `proof` preflight. Structure 2 mm dashed safe (existing). Combined safe overlay when `printReady`. `prod__spec` set / hero / crop. |

Smoke: `npx --yes vite-node scripts/studio-smoke.ts`
